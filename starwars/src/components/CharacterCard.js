import React from 'react';

const CharacterCard = (props) => {
    const {name, hairColor, gender,homeworld,species} = props;

    return (
        <h1>Hello</h1>
    )
}

export default CharacterCard;